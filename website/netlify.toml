# DIAGNOSTIC build command — markers + direct (non-vp) invocation to locate the
# Netlify hang. Restore a clean command once we know where it stalls.
[build]
command = "echo '=== STEP1: extract-api ===' && pnpm exec tsx scripts/extract-api.ts && echo '=== STEP2: vite build (vite@8) ===' && pnpm exec vite build && echo '=== STEP3: build complete ==='"
publish = "dist/client"

[functions]
directory = ".netlify/v1/functions"
